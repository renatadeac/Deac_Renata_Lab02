@page
@model Deac_Renata_Lab02.Pages.Borrowings.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Borrowing</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Borrowing.ID" />

            <!-- Dropdown pentru Membri -->
            <div class="form-group">
                <label asp-for="Borrowing.MemberID" class="control-label"></label>
                <select asp-for="Borrowing.MemberID" class="form-control" asp-items="@(ViewData["MemberID"] as IEnumerable<SelectListItem>)">
                    <option value="">Selectează un membru</option>
                </select>
                <span asp-validation-for="Borrowing.MemberID" class="text-danger"></span>
            </div>

            <!-- Dropdown pentru Cărți -->
            <div class="form-group">
                <label asp-for="Borrowing.BookID" class="control-label"></label>
                <select asp-for="Borrowing.BookID" class="form-control" asp-items="@(ViewData["BookID"] as IEnumerable<SelectListItem>)">
                    <option value="">Selectează o carte</option>
                </select>
                <span asp-validation-for="Borrowing.BookID" class="text-danger"></span>
            </div>

            <!-- Câmp pentru ReturnDate -->
            <div class="form-group">
                <label asp-for="Borrowing.ReturnDate" class="control-label"></label>
                <input asp-for="Borrowing.ReturnDate" class="form-control" />
                <span asp-validation-for="Borrowing.ReturnDate" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
